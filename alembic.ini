"""initial tables with users, payments, roles, and audit logs"""

from alembic import op
import sqlalchemy as sa

# revision identifiers, used by Alembic
revision = "1234567890ab"
down_revision = None
branch_labels = None
depends_on = None

def upgrade():
    # Create users table
    op.create_table(
        "users",
        sa.Column("id", sa.Integer, primary_key=True),
        sa.Column("email", sa.String(255), unique=True, nullable=False),
        sa.Column("password_hash", sa.String(255), nullable=False),
        sa.Column("role", sa.String(50), default="user"),  # roles: user, admin, institution
        sa.Column("locale", sa.String(10)),
        sa.Column("has_paid", sa.Boolean, default=False),
    )

    # Create payments table
    op.create_table(
        "payments",
        sa.Column("id", sa.Integer, primary_key=True),
        sa.Column("user_id", sa.Integer, sa.ForeignKey("users.id")),
        sa.Column("provider", sa.String(50)),
        sa.Column("amount", sa.Integer),
        sa.Column("status", sa.String(50)),
        sa.Column("timestamp", sa.DateTime, server_default=sa.func.now()),
    )

    # Create audit_logs table
    op.create_table(
        "audit_logs",
        sa.Column("id", sa.Integer, primary_key=True),
        sa.Column("user_id", sa.Integer, sa.ForeignKey("users.id")),
        sa.Column("action", sa.String(255), nullable=False),  # e.g., "login", "payment", "role_change"
        sa.Column("timestamp", sa.DateTime, server_default=sa.func.now()),
        sa.Column("details", sa.Text),  # optional extra info
    )

def downgrade():
    op.drop_table("audit_logs")
    op.drop_table("payments")
    op.drop_table("users")